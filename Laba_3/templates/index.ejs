<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Форма регистрации</title>
   <style>
          body {
              font-family: Arial, sans-serif;
              background-color: #f0f8ff; /* Голубой фон */
              color: #333; /* Цвет текста */
              margin: 0 auto;
              display: flex;
              align-items: center;
              justify-content: center;
              height: 100vh;
              width: 30%;
          }

          form {
              background-color: #fff; /* Белый фон для формы */
              padding: 20px;
              border-radius: 8px;
              box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
          }

          input,
          button {
              margin-bottom: 10px;
              padding: 10px;
              width: 100%;
              box-sizing: border-box;
          }

          button {
              background-color: #4da6ff; /* Голубой цвет для кнопки */
              color: #fff;
              cursor: pointer;
          }

          button:hover {
              background-color: #0056b3; /* Темно-голубой цвет при наведении */
          }

          a {
              color: #0056b3; /* Темно-голубой цвет для ссылок */
              text-decoration: none;
          }

          a:hover {
              text-decoration: underline;
          }
      </style>
</head>
<body>
  <form action="/" method="post">
    <input type="text" name="name" value="<%= name %>" placeholder="Имя">
    <input type="text" name="surname" value="<%= surname %>" placeholder="Фамилия">
    <input type="number" name="age" value="<%= age %>" placeholder="Возраст">
    <input type="email" name="email" value="<%= email %>" placeholder="Электронная почта">
    <input type="text" name="city" value="<%= city %>" placeholder="Город проживания">
    <input type="password" name="password" value="<%= password %>" placeholder="Пароль">
    <button type="submit">Зарегистрироваться</button>
  </form>
</body>
</html>
